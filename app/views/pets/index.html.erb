<%= link_to "Found a lost pet? You came to the right place (:", new_pet_path %>

<ul>
  <% @pets.each do |pet| %>
    <li><%= link_to pet.name, pet_path(pet) %> (<%= pet.species %>)</li>
  <% end %>
</ul>